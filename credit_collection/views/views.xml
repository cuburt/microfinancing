<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="credit_collection.list">
      <field name="name">credit_collection list</field>
      <field name="model">credit_collection.credit_collection</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="credit_collection.action_window">
      <field name="name">credit_collection window</field>
      <field name="res_model">credit_collection.credit_collection</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="credit_collection.action_server">
      <field name="name">credit_collection server</field>
      <field name="model_id" ref="model_credit_collection_credit_collection"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="credit_collection" id="credit_collection.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="credit_collection.menu_1" parent="credit_collection.menu_root"/>
    <menuitem name="Menu 2" id="credit_collection.menu_2" parent="credit_collection.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="credit_collection.menu_1_list" parent="credit_collection.menu_1"
              action="credit_collection.action_window"/>
    <menuitem name="Server to list" id="credit_collection" parent="credit_collection.menu_2"
              action="credit_collection.action_server"/>
    -->
  </data>
</odoo>